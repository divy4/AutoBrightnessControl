<?xml version="1.0" encoding="utf-8"?>
<resources>

    <integer name="seekBarMax">200</integer>

    <!-- limits -->
    <item name="minBrightness" format="float" type="dimen">0</item>
    <item name="maxBrightness" format="float" type="dimen">100</item>

    <item name="minUpdateFreq" format="float" type="dimen">0.5</item>
    <item name="maxUpdateFreq" format="float" type="dimen">10</item>

    <item name="minTransition" format="float" type="dimen">0.5</item>
    <item name="maxTransition" format="float" type="dimen">5</item>

    <!-- graph settings -->
    <!-- the number of ways each axis on a graph should be divided -->
    <integer name="graphAxisSections">20</integer>
    <item name="minGraphDotDist" format="float" type="dimen">0.05</item>

    <!-- other -->
    <integer name="seekBarPrecision">1</integer>

</resources>